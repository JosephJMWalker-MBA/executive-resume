{
  "name": "professional-provenance-resume",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Source-controlled resume and portfolio publishing system.",
  "scripts": {
    "build": "node scripts/build-html.mjs",
    "render": "node scripts/build-html.mjs && node scripts/render-pdf.mjs",
    "pdf": "npm run render"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
